{
  "name": "edge-proxy-ui",
  "private": true,
  "description": "Frontend build pipeline for edge-proxy admin UI (Tailwind + daisyUI).",
  "scripts": {
    "build": "tailwindcss -i ./web/input.css -o ./internal/web/static/tailwind.css --minify",
    "watch": "tailwindcss -i ./web/input.css -o ./internal/web/static/tailwind.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "daisyui": "^4.12.14",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17"
  }
}
